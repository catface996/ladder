kind: Gateway
apiVersion: networking.istio.io/v1alpha3
metadata:
  name: cat-gateway
spec:
  servers:
    - port:
        number: 80
        protocol: HTTP
        name: http
      hosts:
        - 'cat.catface996.com'
  selector:
    istio: ingressgateway
status: {}